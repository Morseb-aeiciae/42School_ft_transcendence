// import { useState } from "react";
import SearchBar from "./SearchBar";

const AllChats = () => {
  //   const [chats, setChats] = useState([]);
  // const [test, setTest] = useState(0);

  return (
    <section>
      <div>AllChats comming soon</div>
      <SearchBar></SearchBar>
      {/* <div> test : {test} </div>
      <button
        onClick={() => {
          setTest(test + 1);
        }}
      >
        +1
      </button> */}
    </section>
  );
};

export default AllChats;
